# This is a package file which contains all the packages for this virtual assistant project.
# You need to install this from your command prompt (terminal) incase you don't have any of this packages.
# Before debugging this code make sure to check whether all the packages are installed correctly.

import pyttsx3                                          # pip install pyttsx3
import datetime 
import wikipedia                                        # pip install wikipedia
import speech_recognition as sr                         # pip install SpeechRecognition
import smtplib  
import webbrowser as wb                                 # pip install webbrowser
import os 
import pyautogui                                        # pip install pyautogui
import psutil                       
import pyjokes                                          # pip install pyjokes
